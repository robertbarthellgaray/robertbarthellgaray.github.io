# robertbarthellgaray.github.io

Personal portfolio built with React, Vite, Three.js, and React Three Fiber.

## Development

```bash
npm install
npm run dev
```

Create a production build with `npm run build`.
